# Gradio 6.x in the same conda env needs Starlette ≥0.40 (TemplateResponse API); keep FastAPI in sync.
fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.12.1
pydantic==2.5.0
pydantic-settings==2.1.0
python-dotenv==1.0.0
# 0.0.18+ ships both `multipart` and `python_multipart` (Gradio 6.x imports the latter)
python-multipart>=0.0.18,<0.0.23
supabase==2.0.3
httpx>=0.24.0,<0.25.0
pillow==10.1.0
numpy==1.26.2
torch>=2.0.0
torchvision>=0.18.0
transformers>=4.30.0
accelerate>=0.20.0
safetensors>=0.3.0
# Optional but recommended for Qwen 3.5 Vision (faster load + inference):
# pip install unsloth

